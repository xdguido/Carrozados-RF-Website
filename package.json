{
  "name": "carrozados",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "dev": "rm -rf ./dev && rm -rf ./.cache && parcel src/index.html --out-dir dev",
    "build": "rm -rf ./dist && rm -rf ./.cache && parcel build src/index.html --out-dir dist --public-url ./"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "smooth-scrollbar": "^8.7.4"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.5"
  }
}
